

default_prompt = """
Role: 你是一位资深的知识库管理员，擅长从非结构化文本中提炼核心知识点。

Task: 请阅读下方的 Markdown 文档，从中提取高质量的问答对（QA Pairs）。

Guidelines:

价值导向： 重点提取涉及核心概念、操作步骤、常见问题（FAQ）以及故障排除的内容。

独立性： 每个回答必须表述完整，不依赖上下文。如果问题中涉及“这个项目”、“它”，请替换为具体名称。

颗粒度： 避免过于笼统的问题（如“文档说了什么？”,"文档中的图片说明了什么内容？"），应聚焦于具体知识点。

图文并茂：必须保留文本中所有的图片链接（格式如 ![](_images/image_xx.png)），并将它们精准地插入到对应的回答内容下方，以辅助说明。

完整覆盖：QA Pairs需要完整覆盖文档中的所有知识点，不能遗漏。可以包含大标题大问题和子标题小问题。

Strict Rules (必须严格遵守):
1. 图片占位符强制保留：原始文本中所有以 ![](_images/...) 格式出现的图片链接是核心资产，严禁删除、修改或忽略。
2. 位置精准还原：图片链接必须出现在它原本对应的文字段落之后，作为回答的一部分。
3. Markdown 格式：使用标准的 Markdown 语法。如果图片链接本身已经带有 ![]()，请直接原样输出，不要对其进行转义。

格式要求： 请统一使用 Markdown 列表输出，格式如下：

```markdown
- Q: [在这里输入问题]
  A: [在这里输入详尽、准确的回答]
```

Input Document: 
```markdown
$DOC_CONTENT$
```
"""


text_begin_prompt = """
下面文档中，跳过目录，正文从哪里开始, 需要定位到唯一的标识。
输出格式：{
    start_text: '',
    find_regex: '',
}

Input Document: 
```markdown
$DOC_CONTENT$
```
"""